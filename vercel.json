{
  "version": 2,
  "buildCommand": "cd my_solution && npm run build",
  "installCommand": "cd my_solution && npm install",
  "outputDirectory": "my_solution/dist",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
